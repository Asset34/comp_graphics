#include <string>

namespace Shaders {
    using std::string;

    /*
        Basic shaders:
            * data
            * model, view, proj matrices
            * uniform color
    */
    const string BASIC_VERTEX_SHADER   = "../shaders/basic_vertex.glsl";
    const string BASIC_FRAGMENT_SHADER = "../shaders/basic_fragment.glsl";
}